export function formatcurrency(priceCents) {
    return (priceCents / 100).toFixed(2);
}

export default formatcurrency;
// aanathi aapde {}  bracket vagar import kari shakiye  import directly formatcurrency